const test = require('./test');
const node = require('./node');
const graph = require('./graph');
const menuTemplate = require('./menu');
// and can continue in this fashion in order to keep main.js clean.

module.exports = {
  test,
  node,
  graph,
  menuTemplate
};
